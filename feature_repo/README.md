This directory contains Feast repo objects:
- feature_store.yaml
- entities.py
- data_sources.py
- feature_views.py
- repo.py

Run `feast apply` from the project root via `make apply` or manually:

```
cd feature_repo
feast apply
```
