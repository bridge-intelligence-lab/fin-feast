[pytest]
markers =
    e2e: end-to-end tests that may require docker and network
    integration: integration tests that may start services like Redis/Docker
    network: tests that require network access
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
