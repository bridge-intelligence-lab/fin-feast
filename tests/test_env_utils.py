from __future__ import annotations\n\nimport os\nfrom pathlib import Path\n\nimport pytest\n\nfrom fin_feast.utils.env import (\n    get_zone,\n    resolve_base_path,\n    get_repo_root,\n    get_paths,\n    get_redis_cfg,\n    get_retention_days,\n)\n\n\ndef test_get_zone_valid(monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.setenv(\"FEAST_DATA_ZONE\", \"current\")\n    assert get_zone() == \"current\"\n    monkeypatch.setenv(\"FEAST_DATA_ZONE\", \"experiment\")\n    assert get_zone() == \"experiment\"\n\n\ndef test_get_zone_invalid(monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.setenv(\"FEAST_DATA_ZONE\", \"invalid\")\n    with pytest.raises(ValueError):\n        get_zone()\n\n\ndef test_resolve_base_path_current(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.setenv(\"FEAST_REPO_ROOT\", str(tmp_path))\n    base = resolve_base_path(\"current\", None)\n    assert base == tmp_path / \"data\" / \"offline\" / \"current\"\n\n\ndef test_resolve_base_path_experiment(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.setenv(\"FEAST_REPO_ROOT\", str(tmp_path))\n    with pytest.raises(ValueError):\n        resolve_base_path(\"experiment\", None)\n    base = resolve_base_path(\"experiment\", \"exp1\")\n    assert base == tmp_path / \"data\" / \"offline\" / \"experiments\" / \"exp1\"\n\n\ndef test_get_repo_root_override(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.setenv(\"FEAST_REPO_ROOT\", str(tmp_path))\n    assert get_repo_root() == tmp_path.resolve()\n\n\ndef test_get_paths_structure(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.setenv(\"FEAST_REPO_ROOT\", str(tmp_path))\n    p = get_paths()\n    assert p.data_offline == tmp_path / \"data\" / \"offline\"\n    assert p.data_current_daily == tmp_path / \"data\" / \"offline\" / \"current\" / \"daily\"\n    assert p.data_current_minute == tmp_path / \"data\" / \"offline\" / \"current\" / \"minute\"\n    assert p.data_experiments == tmp_path / \"data\" / \"offline\" / \"experiments\"\n\n\ndef test_get_redis_cfg(monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.delenv(\"REDIS_HOST\", raising=False)\n    monkeypatch.delenv(\"REDIS_PORT\", raising=False)\n    assert get_redis_cfg() == (\"localhost\", 6379)\n    monkeypatch.setenv(\"REDIS_HOST\", \"redis\")\n    monkeypatch.setenv(\"REDIS_PORT\", \"6380\")\n    assert get_redis_cfg() == (\"redis\", 6380)\n\n\ndef test_get_retention_days(monkeypatch: pytest.MonkeyPatch) -> None:\n    monkeypatch.delenv(\"DAILY_RETENTION_DAYS\", raising=False)\n    monkeypatch.delenv(\"MINUTE_RETENTION_DAYS\", raising=False)\n    assert get_retention_days() == (400, 30)\n    monkeypatch.setenv(\"DAILY_RETENTION_DAYS\", \"7\")\n    monkeypatch.setenv(\"MINUTE_RETENTION_DAYS\", \"3\")\n    assert get_retention_days() == (7, 3)\n
