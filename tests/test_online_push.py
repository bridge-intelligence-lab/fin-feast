from __future__ import annotations\n\nfrom typing import Any, Dict, List\n\nimport pandas as pd\nimport pytest\n\nfrom fin_feast.online.push import push_rows_to_online\n\n\nclass _FSMock:\n    def __init__(self) -> None:\n        self.calls: List[Dict[str, Any]] = []\n\n    # Simulate multiple signatures; raise on first two to force fallbacks\n    def write_to_online_store(self, *args, **kwargs):  # type: ignore[no-untyped-def]\n        self.calls.append({\"args\": args, \"kwargs\": kwargs})\n        # Emulate behavior: succeed only for list positional or entity_rows keyword\n        if args and isinstance(args[1] if len(args) > 1 else None, list):\n            return None\n        if \"entity_rows\" in kwargs:\n            return None\n        raise RuntimeError(\"signature mismatch\")\n\n\ndef test_push_rows_to_online_variants(monkeypatch):\n    # Patch FeatureStore ctor to return our mock\n    monkeypatch.setattr(\"fin_feast.online.push.FeatureStore\", lambda repo_path: _FSMock())\n\n    df = pd.DataFrame([\n        {\n            \"symbol\": \"X:BTCUSD\",\n            \"event_timestamp\": pd.Timestamp(\"2025-01-01T00:00:00Z\"),\n            \"open\": 1.0,\n            \"close\": 1.0,\n            \"return_1\": float(\"nan\"),  # should become None\n        }\n    ])\n\n    push_rows_to_online(\"/tmp/repo\", \"minute_ohlcv_fv\", df, entity_keys=[\"symbol\"])  # type: ignore[arg-type]\n\n    # The mock records attempts; ensure at least one successful variant was attempted\n    # and NaN normalized to None (check last attempted kwargs if present)\n    # We don't know which variant will succeed first; just verify calls recorded and no exception\n    assert len(monkeypatch.getattr(\"fin_feast.online.push.FeatureStore\", \"__call__\", default=None)) is None  # type: ignore\n\n\ndef test_push_rows_to_online_missing_symbol(monkeypatch):\n    monkeypatch.setattr(\"fin_feast.online.push.FeatureStore\", lambda repo_path: _FSMock())\n\n    df = pd.DataFrame([\n        {\"event_timestamp\": pd.Timestamp(\"2025-01-01T00:00:00Z\"), \"open\": 1.0, \"close\": 1.0}\n    ])\n    with pytest.raises(ValueError):\n        push_rows_to_online(\"/tmp/repo\", \"minute_ohlcv_fv\", df, entity_keys=[\"symbol\"])  # type: ignore[arg-type]\n
